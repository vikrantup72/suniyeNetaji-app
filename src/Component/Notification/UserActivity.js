import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Header from '../../utils/Header'

const UserActivity = () => {
  return (
    <View>
      <Header HeaderTxt={'UserActivity'}/>
    </View>
  )
}

export default UserActivity

const styles = StyleSheet.create({})